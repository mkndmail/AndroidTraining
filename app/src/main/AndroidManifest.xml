<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.layoutdesigns">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LayoutDesigns">
        <activity android:name=".activities.RecyclerDatabaseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentRecordActivity">

        </activity>
        <activity android:name=".activities.AddNameActivity" />
        <activity android:name=".activities.FragmentTest" />
        <activity android:name=".activities.RecyclerStudentSecondActivity" />
        <activity android:name=".activities.StudentActivity" />
        <activity android:name=".activities.EmployeeListActivity" />
        <activity android:name=".activities.ScoreCardActivity" />
        <activity android:name=".activities.SecondActivity" />
        <activity android:name=".activities.MainActivity"></activity>
    </application>

</manifest>